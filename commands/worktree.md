---
description: "Create a new git worktree."
subtask: false
---

Load the `worktree` skill and follow its instructions precisely.

User context: $ARGUMENTS

Run the `worktree` skill against the specified topic.